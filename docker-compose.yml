version: '3.7'
services:

  maching-prefixes:
    image: my-favorite-artist
    container_name: my-favorite-artist-container
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - CONFIG_PATH=config/local
    ports:
#       REST server
      - "8080:8080"
    networks:
      - mp
networks:
  mp:
    driver: bridge